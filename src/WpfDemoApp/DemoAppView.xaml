<Window x:Class="WpfDemoApp.DemoAppView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="Exception Reporter Demo App (WPF)" Height="300" Width="300"
    WindowStyle="ThreeDBorderWindow"
    SizeToContent="WidthAndHeight" 
    WindowStartupLocation="CenterScreen"
    AllowsTransparency="False"
    ShowInTaskbar="True" 
    Topmost="False"
    ResizeMode="CanResizeWithGrip">

    <Window.Resources>
        <Style TargetType="{x:Type TextBlock}">
            <Setter Property="FontFamily" Value="Trebuchet MS" />
            <Setter Property="FontSize" Value="14" />
            <Setter Property="Foreground" Value="Blue" />
            <Setter Property="Foreground" Value="Blue" />
            <Setter Property="HorizontalAlignment" Value="Center" />
            <Setter Property="Margin" Value="5" />
        </Style>
    </Window.Resources>

    <StackPanel Name="mainStackPanel" Margin="0,20">
        <TextBlock>
            <Hyperlink >Default Exception Report</Hyperlink>
        </TextBlock>
        <TextBlock>
            <Hyperlink>Custom Exception Report</Hyperlink>
        </TextBlock>
        <TextBlock>
            <Hyperlink>Custom Exception Report</Hyperlink>
        </TextBlock>
        <TextBlock>
            <Hyperlink>Multiple Exceptions Report</Hyperlink>
        </TextBlock>
    </StackPanel>
</Window>
